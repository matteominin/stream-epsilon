#logging:
  #level:
    #root: DEBUG

spring:
  config:
    activate:
      on-profile: test

  data:
    mongodb:
      uri: ${MONGO_TEST_URI}



mongodb-vector-search-demo:
  uri: ${MONGO_VECTOR_SEARCH_DEMO_URI}